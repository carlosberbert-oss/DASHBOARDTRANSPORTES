[build]
  # site estático: sem etapa de build; serve a raiz do repo
  publish = "."
  command = ""
  # pasta das funções serverless
  functions = "netlify/functions"

[build.environment]
  # Node 18+ garante 'fetch' nativo usado pela função
  NODE_VERSION = "18"
